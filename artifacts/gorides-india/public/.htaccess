<IfModule mod_rewrite.c>
  RewriteEngine On

  # Serve existing files and folders normally.
  RewriteCond %{REQUEST_FILENAME} -f [OR]
  RewriteCond %{REQUEST_FILENAME} -d
  RewriteRule ^ - [L]

  # Let the client-side router handle all other paths.
  RewriteRule ^ index.html [L]
</IfModule>